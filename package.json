{
  "name": "dbms-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=client && rm -rf dist && mv client/dist dist",
    "dev": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "preview": "npm run preview --workspace=client"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0"
  }
}